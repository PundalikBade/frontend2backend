🌟 What is querySelector?
querySelector is a method in JavaScript used to select the first matching HTML element based on a CSS selector.

It’s part of the Document Object Model (DOM) API and is super handy for accessing or manipulating elements in the HTML document.

